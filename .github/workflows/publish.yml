name: Build, Publish, and Deploy Storybook

on:
  release:
    types: [published]

permissions:
  contents: read
  id-token: write
  pages: write

jobs:
  build_and_publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          registry-url: https://registry.npmjs.org/
      # Ensure npm 11.5.1 or later is installed - https://docs.npmjs.com/trusted-publishers#github-actions-configuration
      - name: Update npm
        run: npm install -g npm@latest
      - name: Set package.json version from release tag
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          VERSION=${VERSION#v}
          npm version --no-git-tag-version "$VERSION"
        env:
          GITHUB_REF: ${{ github.ref }}
      - run: npm install
      - run: npm run build
      - run: npm publish
      - run: npm --prefix ./storybook install
      - run: npm run build-storybook
      - uses: actions/upload-pages-artifact@v3
        with:
          path: ./storybook/storybook-static

  deploy_storybook:
    needs: build_and_publish
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
