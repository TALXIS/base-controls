export const formTranslations = {
    requiredFieldError: "This field is required.",
    maxLengthError: "Maximum length is {{ max }} characters.",
    minValueError: "Value must be at least {{ min }}.",
    maxValueError: "Value must be at most {{ max }}.",
} as const;
