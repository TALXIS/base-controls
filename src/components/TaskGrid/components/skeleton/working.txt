export const Skeleton = ({ height }: ISkeletonProps) => {
    return <div style={{display: 'flex', flexDirection: 'column', height: '100%'}}>
        <div>header</div>
        <div style={{flexGrow: 1, overflow: 'auto', height: 0}}>
            <div style={{height: 3000, background: 'red', width: '100%'}}>inner content</div>
        </div>
        <div>footer</div>
    </div>

};