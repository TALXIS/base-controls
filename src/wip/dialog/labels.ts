export interface IDialogLabels {
    headerText?: string;
    primaryButtonText: string;
    dismissButtonText: string;
}

export const DIALOG_LABELS: IDialogLabels = {
    primaryButtonText: 'OK',
    dismissButtonText: 'Dismiss',
}