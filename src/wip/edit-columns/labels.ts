export interface IEditColumnsLabels {
    columnSource: string;
    noName: string;
    header: string;
    addColumn: string;
    noResults: string;
}

export const EDIT_COLUMNS_LABELS: IEditColumnsLabels = {
    columnSource: 'Column source',
    noName: 'No name',
    header: 'Edit columns',
    addColumn: 'Add column',
    noResults: 'No results found'
}