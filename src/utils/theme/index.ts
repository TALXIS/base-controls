//the generator every control's theme comes from: a consumer theming a cell builds it from the same three
//colours, and gets the whole ramp the Fluent components read rather than the handful it set itself
export { Theming } from "@legacy";
export type { ITheme } from "@legacy";
export * from './ControlTheme';
export * from './hooks';
export * from './components';
